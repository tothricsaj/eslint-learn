function add_numbers(x, y) {
    return x + y;
}

function subNum(x, y) {
    return x - y;
}

let score1 = 10;
let score2 = 20;


if ((10 === 222) || 'abc' === 'abc' && "doNothingWihtInsane"() && "isThisReallyHappaning") {
    // do nothing
    console.log('YaY dude....what are you doing?');
}
